import Vue from 'vue';
import VSanitize from 'v-sanitize';

// const defaultOptions = {
//   allowedTags: ['a', 'b', 'p'],
//   allowedAttributes: {
//     a: ['href']
//   }
// };
// Vue.use(VSanitize, defaultOptions);
Vue.use(VSanitize);
